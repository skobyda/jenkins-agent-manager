<?xml version="1.0" encoding="UTF-8"?>

<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout"
         xmlns:t="/lib/hudson" xmlns:f="/lib/form">
    <f:dropdownDescriptorSelector field="trigger" title="Trigger" descriptors="${instance.triggerDescriptor}" />
    <f:dropdownDescriptorSelector field="condition" title="Condition" descriptors="${instance.conditionDescriptor}" />
    <f:dropdownDescriptorSelector field="action" title="Action" descriptors="${instance.actionDescriptor}" />
</j:jelly>
